using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjetosADropear : MonoBehaviour
{

    public static GameObject Pocion;
    public  GameObject Pocion1;
    // Start is called before the first frame update
    void Start()
    {
        Pocion = Pocion1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
